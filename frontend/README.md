# MujuzPM — Frontend

React 19 + Vite client. See the [root README](../README.md) for full setup, environment variables, and how to run the whole stack.

Quick start (backend must already be running on port 4000):

```bash
npm install
npm run dev
```

Opens on http://localhost:5173, proxying `/api` to the backend.
